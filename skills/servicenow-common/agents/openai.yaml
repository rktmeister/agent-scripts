interface:
  display_name: "ServiceNow Common"
  short_description: "Route general ServiceNow work to the right skill"
  default_prompt: "Use $servicenow-common to choose the right ServiceNow skill and apply the shared trust model before doing deeper implementation work."
